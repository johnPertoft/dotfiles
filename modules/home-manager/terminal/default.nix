# Grouping namespace for the terminal / shell environment: emulator
# (kitty), multiplexer (tmux), fuzzy finder (fzf), and the shells. Each
# subdirectory is exposed as self.homeModules.terminal.<name>.
import ../discover.nix ./.
